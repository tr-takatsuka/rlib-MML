# **Music Macro Language Compiler**

[ENGLISH](/README.md)

## Description

MML を 標準MIDIファイル へコンパイル(変換)するクラスライブラリです。<br>
標準MIDIファイル から MML への変換もサポートしています。
- C++20 での実装です。
- コマンドラインアプリとしてビルドできる形になっています。
  - mmltosmf : MML → 標準MIDIファイル
  - smftomml : 標準MIDIファイル → MML
- MML の文法には決まった規格があるわけではありませんが、暗黙の標準のようなものは踏襲するようにしています。

## Demo Page

[https://rlib-mml.thinkridge.jp/](https://rlib-mml.thinkridge.jp/)

MML の作成から再生までをブラウザ上で行える形にまとめています。

## Requirement

C++20 環境でコンパイルできます。ビルドには boost が必要です。

以下の環境で動作確認しています。

- linux g++
- windows VisualStudio 2019,2022,2026

### WebAssembly

WebAssembly をサポートしています。

# **MML (Music Macro Language) 文法**

## 音符 その他

|  表記  |  説明  |例|
| ---- | ---- | ---- |
|a～[+-][音長]| 音符(ノート)です。<br>直後に + - で半音上げ下げです(省略可)。<br>その後に音長を指定できます(省略可)。 | // 4分音符のファです<br>f4<br><br>// ドレミファソラシです。音長はデフォルト値です。<br>cdefgab <br><br>// 符点16分音符のラ♭です。<br>a-16.|
|r[音長]|  休符です。直後に音長を指定できます(省略可)。 | // 16分休符です。<br>r16|
|^[音長]|  タイです。直後に音長を指定できます(省略可)。<br>直前の音符の音長を足します。<br>直前が音符ではない場合は休符と同じです。| // 4分音符３つ分の長さのドです。<br>c4^4^4<br><br>// 4分音符3つ分の長さのドです。途中でテンポを変えています。<br>// 音符と ^ の間に別の命令を記述することが可能です。<br>c4 t50 ^4 t20 ^4
|l[音長]|  デフォルト音長です。<br>音符及び休符で音長指定を省略した場合に採用する値です| // 8分音符のドレミです<br>l8 cde|
|<<br>> | オクターブアップ(<) ダウン(>) です。<br>1オクターブの範囲は ド(c)～シ(b) の12音です。| // ド と １オクターブ上のド を２回繰り返します<br>c\<c>c\<c |
|o[オクターブ値]| オクターブを指定します。値は -2～8 です<br>MIDIノートナンバー60はC3です。o3c と記述します| // MIDIノートナンバー60(C3)、62(D3)、64(E3) です<br>o3 cde |
|t[テンポ値]| テンポを指定します。値は 1.0～999.0 を指定可です。<br>少数も表記可です。相対指定が可能です。| // テンポ120でドレミ、テンポ90でドレミです<br>t120 cde t-30 cde|
|@[プログラムナンバー]| プログラムナンバー(音色)を指定します。値は0～127です。| // プログラムナンバー3に切り替えてドレミです<br>@3 cde|
|v[ベロシティ値]| ベロシティ(ピアノでいうと鍵盤を叩く強さ)を指定します。<br>値は0～127です。相対指定が可能です。| ベロシティ123のドレミ、ベロシティ113のファソラですです<br>v123 cde v-=10 fga|
|'[和音モード切り替え]| 現在位置を進めないモードに切り替えます。<br>現在位置を進めるには休符を使います。<br>和音のような2音以上を同時に発音する場合に使います。| // 8分音符のドミソの和音と、レファラの和音です。<br>l8 'cegr dfar'|

### 音長
- 音の長さ(ここでは音長と表記します)の指定方法です。

| 表記  |  説明  |例|
| ---- | ---- | ---- |
|1～192<br>音符表記| 〇分音符の値を指定します。<br>12分音符や24分音符という表記も可です| // 4分音符のドです<br>c4|
|!1～!99999<br>ステップ数表記| ステップ数で指定します。4分音符が480です。| // 8分音符のドです<br>c!240|
|.<br>付点表記| 付点〇分音符というところの付点を意味します。<br>複数記述することで複付点音符の意味(直前の音長の半分を足す)になります。| // 付点4分音符のドです<br>c4.<br><br>// 4分音符と8分音符と16分音符と32分音符を足した音長のドです<br>c4...|
|+ - 数値<br>音長の加減算指定|音長値を加減算します。|// 8分音符と4分音符を足した音長のドです<br>c8+4<br><br> // 32分音符のドレ、全音符から32分音符2つ分を短くしたミです。<br>c32d32e1-32-32<br><br>// この場合は、4分音符のレ♭です。ご注意下さい<br>d-4|

## 関数

|  表記  |  説明  |例|
| ---- | ---- | ---- |
|CreatePort(<br>&emsp;name:[ポート名],<br>&emsp;instrument:[楽器名]\(省略可),<br>&emsp;channel:[チャンネル番号],<br>)|ポートを定義(宣言)します<br>チャンネル番号は 1～16 です。|// "Piano"という名称でMIDIチャンネル3のポートを宣言し、切り替えます。<br>// instrument は楽器名を指定します。省略も可能です。<br>CreatePort(name:Piano, instrument:"fm", channel:3)|
|Port([ポート名])|ポート切り替えです|// ポート"Piano" に切り替えてドレミです<br>Port(Piano) cde|
|V([音量値])<br>別名 Volume| 音量です。値は 0～127です。<br>相対指定が可能です。|// ボリューム120でドレミ、90でファソラです<br>V(120) cde V(-=30) fga|
|Ep([値])<br>別名 Expression|エクスプレッションです。値は 0～127です。<br>相対指定が可能です。| // エクスプレッション120でドレミ、90でファソラです<br>Ep(120) cde Ep(-=30) fga|
|Pan([パン値])<br>別名 Panpot| パン(パンポット)です。<br>値は 0(左端)～127(右端)で、中央は64です。<br>相対指定が可能です。| // パン10でドレミ、パン60でファソラです<br>Pan(10) cde Pan(+=50) fga|
|PitchBend([ベンド値])|ピッチベンドです。<br>値は -8192(2音下)～8191(2音上)で、中央は0です。<br>相対指定が可能です。|// 半音下げたドレミ、通常のドレミです<br>PitchBend(-4096) cde PitchBend(+=4096) cde|
|FineTune([セント値])|FineTuneです。半音を100としたセント値を指定します。<br>値は -100.0～100.0で少数値をサポートします。中央は0です。<br>相対指定が可能です。|// 基準の音程から20.5セント上のドレミ、9.5セント下のドレミです<br>FineTune(20.5) cde FineTune(-=30) cde|
|CoarseTune([値])|CoarseTuneです。半音単位でキーを調整します。<br>値は -64～63で、中央は0です。<br>相対指定が可能です。| // 基準の音程からキーを2つ上げたドレミ、1つ下げたドレミです<br>CoarseTune(2) cde CoarseTune(-=3) cde|
|CC([コントロール番号],[値])<br>別名 ContorlChange| コントロールチェンジです。<br>第1引数がコントロール番号、第1引数が値です|// バンクセレクトしたプログラムチェンジです。<br>CC(0,10)CC(32,130)@2|
|CreateSequence(<br>&emsp;name:[シーケンス名],<br>&emsp;mml:[MML],<br>)|シーケンス(サブシーケンス)を定義します。<br>楽曲(MML)を部品として定義し、<br>以降のMMLの中で呼び出す(張り付ける)ことができます。| // ドラムパターンを定義します<br/>CreateSequence(name:drum, mml:"<br/>&emsp;CreatePort(name:kick, channel:10) l8 o1 c^^c ^c^^<br/>&emsp;CreatePort(name:snare, channel:10) l8 o1 ^^d^ ^^d^<br/>")|
|Seq(<br>&emsp;[シーケンス名],<br>&emsp;length:[長さ(省略可)]<br>)<br>別名 Sequence|定義済のシーケンス(サブシーケンス)を呼び出します。| // 定義したドラムパターンを3回繰り返します。<br>// 3周目のシ－ケンスは2分音符分のみ採用します<br/>Seq(drum) Seq(drum) Seq(drum,length:"2")|
|MasterVolume([値])|マスターボリュームです。値は 0～16383 です。相対指定が可能です。<br>システムエクスクルーシブのGMマスターボリュームの別名です| // マスターボリュームを10000にしたあと 1000 上げています<br/>MasterVolume(10000) r MasterVolume(+=1000)|
|SysEx(<br>&emsp;[データ]...<br>)<br>|システムエクスルーシブです。<br>名前ナシの可変長引数でデータを指定します。<br>先頭バイトは 0xf0、0xf7 のいずれかを、末尾は（原則として）0xf7 を指定します| // Roland GS 音源をリセットします（GS Reset）<br/>SysEx(0xf0,0x41,0x10,0x42,0x12,0x40,0x00,0x7f,0x00,0x41,0xf7)|
|Meta(<br>&emsp;type:[イベントタイプ],<br>&emsp;[データ]...<br>)|メタイベントです。<br>typeでイベントタイプを指定します。<br>名前ナシの可変長引数でデータを指定します。<br>データ長を記述する必要はありません。| // title 情報です<br/>Meta(type:0x1,"The Lost King's Scepter")<br>// SMPTE オフセットです。<br>Meta(type:0x54,96,0,0,0,0)|
|DefinePresetFM(<br>&emsp;no:[プログラムナンバー],<br>&emsp;name:[音色名],<br>&emsp;[音色データ]...<br>)|rlib-MML でFM音源音色を定義するシーケンサー固有のメタイベントです。|DefinePresetFM(no:4, name:"piano",<br>// AR  DR  SR  RR  SL  TL KS  ML DT<br>   29,  8,  0,  8,  3, 31, 2,  1, 3,<br>   31,  3,  1,  6, 10,  0, 0,  2, 7,<br>   29, 20,  0,  9,  2, 44, 0,  4, 2,<br>   31,  7,  2,  6,  6,  0, 0,  1, 5,<br>// AL  FB<br>    4,  7,<br>)|
|DefinePresetPSG(<br>&emsp;no:[プログラムナンバー],<br>&emsp;name:[音色名],<br>&emsp;[音色データ]...<br>)|rlib-MML でPSG音源音色を定義するシーケンサー固有のメタイベントです。|DefinePresetPSG(no:5, name:"piano",<br>// AR   HR    DR   SL   RR		noise	tone<br>  0.0, 0.0,  1.0, 0.3, 1.0,		   -1,     1,<br>)<br>// AR,HR,DR,SL,RR : ソフトウェアエンベロープの設定値<br>// noise : ノイズ周波数(0～31) -1=OFF<br>// tone : トーン 1=ON/0=OFF|




## 文字列

文字列を指定する箇所では、以下の書式が使えます。

|  表記  |  説明  |例|
| ---- | ---- | ---- |
| ○○○ | 頭文字が英字で、かつ英数字のみ使われている文字列はこの指定が可能です。| trumpet |
| "○○○" | " から " までが文字列です。空白や記号を使う場合にも使えます。　| "drum part"
| R"\*\*(○○○)\*\*" | 空白や記号や " を使う場合にも使えます。<br>シーケンスの中にシーケンスを定義するような場合は、** を一意の文字列にすることで、文字列定義の中にさらに文字列定義があるようなケースに対応できます　| R"(drum)"<br><br> R"ch1( mml:R"(drum)" )ch1"

## 相対指定

相対指定が可能な箇所では、現在値を基準に値を指定できます。
複合演算子で四則演算をサポートします。少数表記も可能です。

|  表記  |  説明  | 例 |
| ---- | ---- | ---- |
| +=n | 現在値にnを足します| // ベロシティを+10しています<br> v+=10 cde |
| -=n | 現在値からnを引きます| // エクスプレッション100 → 96 (95.6を四捨五入) → 95(95.2を四捨五入)<br>Ep(100) c Ep(-=4.4) d Ep(-=0.4) e|
| \*=n | 現在値にnを掛けます| // ボリューム100 → 127 (130が範囲内にクリップされます) →  117 (現在値130に0.9を掛けます)<br>V(100) c V(\*=1.3) d V(\*=0.9) e|
| \/=n | 現在値をnで割ります| // ボリューム100 → 50 → 25<br>V(100) c V(/=2) d V(/=2) e|


## コメント

|  表記  |  説明  |例|
| ---- | ---- | ---- |
| // ○○○| １行コメントです。// 以降、改行までがコメントです。| // ここがコメントです |
| /* ○○○ */ | 範囲コメントです。<br>/* から */ までがコメントです。複数行に跨ぐコメントも可です。| /* ここが<br>コメントです */|

# Licence

[LICENSE](/LICENSE)

